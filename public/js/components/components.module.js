(function(){
'use strict';
/**
 * @ngdoc module
 * @name app.components
 * @description
 * # app.components
 * Rollup Module for all feature (not layout/structural) components
 * Possibly for global components only, to avoid bloating of components directory
 */
angular.module( 'app.components', [
 'app.components.litProChart',
] );
}());

